spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/menu_manager
    username:
    password:
  jpa:
    properties:
      javax:
        persistence:
          schema-generation:
            scripts:
              action: create
              create-target: create_tables.sql
              create-source: metadata